# Example environment variables for BetterDiscordPanel
SEED_ADMIN_PASSWORD=supersecurepassword
SEED_ADMIN_EMAIL=admin@bdp.emkacz.dev
# Seed admin (optional)

# Generate a 32 byte key and provide as base64: e.g. in bash:
#   openssl rand -base64 32 | sed 's/.*/base64:&/'
ENCRYPTION_KEY=base64:REPLACE_WITH_BASE64_32_BYTES
JWT_SECRET=change_this_jwt_secret
# Security

REDIS_URL=redis://redis:6379
# Redis

POSTGRES_PASSWORD=bdpchange
DATABASE_URL=postgresql://bdp:bdpchange@postgres:5432/bdp?schema=public
# Database

FRONTEND_URL=https://bdp.emkacz.dev
VITE_API_ORIGIN=https://api.bdp.emkacz.dev
API_URL=https://api.bdp.emkacz.dev
API_PORT=3000
NODE_ENV=production
# App

# Optionally set cookie domain for cross-subdomain cookies (e.g. .bdp.emkacz.dev)
COOKIE_DOMAIN=.bdp.emkacz.dev
# Possible values: 'none', 'lax', 'strict'
COOKIE_SAMESITE=none

# Copy to .env and fill in values for production
